# القرارات وخريطة الطريق — My Assistant

> **الغرض:** نقطة استئناف واحدة موثوقة. لو فتحت جلسة جديدة، اقرأ الملف ده الأول
> عشان تعرف: إيه اتقرر وليه، إيه الجاري، وإيه المؤجّل — من غير ما تعيد قراءة كل حاجة.
>
> **ملفات مكمّلة:** التفاصيل اليومية في `SESSION_HANDOVER.md` · بنك الأفكار الكامل
> والمقايسات في `ROADMAP_ESTIMATES.md` · قواعد المعمار في `CLAUDE.md`.
>
> **آخر تحديث:** 2026-07-21

---

## ★ أحدث جلسة (2026-07-20/21) — الحذف النهائى، المراجعة، وتأمين البيانات

**الحالة النهائية:** DB **v59** · `flutter analyze` = **0** · `flutter test` =
**310/310** · APK **٤٦.٤ ميجا** (كان ٧٢.٢) · آخر commit `d2a0c48` على `main` ·
نقطة رجوع للكود: tag **`before-purge-18-screens`**.

### أ) تمّ — بالترتيب

1. **الحذف النهائى** (`9d8c485`): ١٨ شاشة + ١٨ repo/core + ١٥ موديل + **١٦ جدول**
   (migration **v59** = `DROP TABLE IF EXISTS`). المستخدم اختار صراحةً مسح
   **البيانات كمان** بعد ما اتعرض عليه إن ده مايترجعش فيه. −٧٠٧٦ سطر.
2. **مراجعة السايدبار بعد الحذف** (`3ca76c2`) — لقت **بندين مكسورين فعلاً**:
   «تقرير PDF مخصّص» و«تقويم النتيجة» كانوا لسه بيعرضوا «العدادات»
   و«الواجبات الاجتماعية» كخيارات وبياناتها اتمسحت (أقسام فاضية / فلتر ميت).
   \+ نصوص قديمة (الجولة التعريفية · مساعدة العقل) + كود ميت.
3. **توحيد العناوين** (`890c18b`): «صلاتى» و«ملابسى» كانت الشاشة جوّه لسه
   بعنوانها القديم.
4. **جولة فتح كل شاشات السايدبار على الجهاز** (`b81c9af`) — **مفيش شاشة مكسورة**،
   والـlogcat نضيف من `❌`/`E/flutter` فى كل الدفعات.
5. **تأمين النسخ الاحتياطى + تصغير الـAPK** (`b29b4ee`) — تفاصيلها فى (ج) و(د).

### ب) ★★ قرار حاكم جديد — **المستخدم بيقلّم، مش بيوسّع**

٦ من الـ١٨ بند اللى اتمسحوا **اتبنوا فى نفس الجلسة**. القاعدة دلوقتى:
**ما تقترحش ولا تبنى بنود سايدبار جديدة من نفسك**؛ ولو اتطلب بند، اسأل هيقعد
فين. الافتراضى: «أقل بنود، أوضح».

### ج) 🔴 ثغرة اتقفلت — مسار الاستعادة كان ممكن يضيّع البيانات

`restoreBackup` كان بيكتب بايتات النسخة **فوق القاعدة الحيّة على طول**، من غير
أى تحقق. ملف تالف أو كتابة بتقف فى النص = البيانات تروح والنسخة ماجتش.
دلوقتى `BackupService.applyBackupBytes` (مفصولة عن منتقى الملفات عشان تتّست):
تحقّق من توقيع SQLite → ملف مؤقت جنبها → نسخة أمان `.before_restore` →
**rename ذرّى**. أى فشل قبل آخر خطوة = البيانات زى ما هى بالظبط.
**+٣ تستات** — أول تغطية للمسار ده أصلاً (دورة كاملة · ملف تالف بيترفض والقاعدة
مااتغيّرتش ولا بايت · `looksLikeSqlite`).

### د) الـAPK ٧٢.٢ ← ٤٦.٤ ميجا (−٣٦٪)

مكتبات ML Kit بتيجى **بكل المعماريات** حتى مع `--target-platform android-arm64`
(ده بيفلتر مكتبات فلاتر نفسها بس) → ٢٥.٧ ميجا مكتبات الجهاز ما يقدرش يشغّلها.
الحل = `packaging { jniLibs { excludes += [...] } }` فى `build.gradle.kts`.
**⚠️ `ndk.abiFilters` مابيشتغلش هنا** (بيتعارض مع `--split-per-abi`، وفلاتر
بيتحكم فى التغليف بنفسه).

### هـ) الشاشة الرئيسية — مافيش شغل كود مطلوب

الـ**١٤ قسم** (يومك فى سطر · الأزرار السريعة · الصلاة/الملخص · نظرة الأسبوع ·
الدورة · بيانات الساعة · الفواتير · المستندات · مواعيد النهاردة · الأدوية ·
الوجبات · العادات · قفل اليوم · أقسامك) **كلهم بيتخفوا ويترتّبوا من الإعدادات**
(«الصفحة الرئيسية»)، وكل قسم مابيظهرش إلا لما يبقى فيه محتوى. مفيش حاجة تتشال
إلا لو المستخدم طلب مسح نهائى لقسم معيّن.

### و) الجارى / المفتوح (للاستكمال)

1. **اسم التطبيق** — لسه مفتوح؛ المستخدم بيختار. لما يستقر: label الأندرويد +
   الأونبوردنج + العناوين عربى/إنجليزى.
2. **لغة التطبيق اتغيّرت للإنجليزى بالغلط** أثناء المسح الآلى — رجوعها دوسة
   واحدة على «العربية» فى فوتر الدرج (محتاجة المستخدم).
3. **تجربة ميدانية بشرية** للبنود التسعة اللى اتبنت (اتأكدنا إنها بتفتح، بس
   مااتجربتش ببيانات حقيقية على مدى أيام).
4. **اختيارى:** مسح نهائى لأى قسم من الرئيسية (زى ما اتعمل للسايدبار).
5. الشاشات المحذوفة **كودها** قابل للرجوع من tag `before-purge-18-screens` —
   لكن **بياناتها راحت خلاص** (إلا لو فيه نسخة احتياطية قديمة).

### ز) جوتشا اتكشفت الجلسة دى

- **«شِيل من السايدبار» ≠ «كود ميت»** — grep على اسم الكلاس قبل أى حذف؛ ٥ بنود
  كانت لسه موصولة من مكان تانى، و٧ من البحث الشامل بس.
- **`flutter analyze` بيمسك المراجع الميتة بس، مش استعلام SQL على جدول اتمسح** —
  ده بينفجر وقت التشغيل. لازم grep يدوى لأسماء الجداول + تشغيل فعلى.
- **الجهاز قابل للطى وفيه شاشتين** — `screencap` من غير `-d <displayId>` بيرجّع
  لقطات من الشاشة المطفية (تطبيقات تانية/سواد). هات الرقم من `dumpsys display`
  وشغّل بـ`am start -n` مش `monkey`.
- **نافذة البصمة (`BiometricPromptRoot`) مابتتصوّرش** — «كلمات السر» بتطلع سودا
  وده سلوك صح.
- **🔴 مامسحش شاشات بالتخبيط العشوائى** — الدوسات العمياء غيّرت **لغة التطبيق**
  (زرار «English» فى فوتر الدرج) وكتبت رقم فى خانة. أى مسح لازم يبقى: تشغيل
  نضيف لكل شاشة + إحداثيات متأكد منها + سكرين شوت للتأكيد.
- الـBash هنا **Git Bash** (مش PowerShell) → `git commit -F - <<'MSG'`.
- كونسول بايثون بـcp1252 **بيقع على العربى** → اكتب لملف بدل `print`.

---

## جلسة (2026-07-19/20) — ٩ بنود من بنك الأفكار، وبعدين **تقليم السايدبار**

> **⚠️ أهم قرار فى الجلسة دى (اقراه قبل ما تضيف أى حاجة):** المستخدم **بيقلّم**
> السايدبار، مش بيوسّعه. فى نفس الجلسة اللى اتبنى فيها ٩ بنود جداد، طلب بعدها
> **شيل ١٧ بند** من السايدبار — **منهم ٦ بنود اتبنوا فى نفس الجلسة**
> (أظرف المرتب · سعر الدولار · أرقامك القياسية · اليوم المثالى · مراجعة القرآن ·
> الكبسولة الزمنية). يعنى: **بلاش تقترح/تبنى بنود سايدبار جديدة من نفسك**، ولو
> اتطلب بند جديد اسأل الأول هيقعد فين، والافتراضى بقى «أقل بنود، أوضح».

**الحالة النهائية:** `flutter analyze` = **0** · `flutter test` = **330/330** ·
**DB v58** · APK **72.4MB** (arm64) · آخر commit `23f6e38` على `main`.

### أ) اتبنى — ٩ بنود من بنك الأفكار (`ROADMAP_ESTIMATES.md`)

كل بند اتعمله نفس الدورة: analyze → test → APK → commit.

| البند | الملفات | DB |
|---|---|---|
| أرقامك القياسية | `core/personal_records.dart` + `screens/growth/records_screen.dart` | — |
| اليوم المثالى | `core/perfect_day.dart` + `screens/growth/perfect_day_screen.dart` | — |
| مراجعة/حفظ القرآن | `data/memorization_repo.dart` + `screens/worship/memorization_screen.dart` | **v56** |
| أظرف المرتب | `core/salary_plan.dart` + `screens/money/salary_envelopes_screen.dart` | — |
| رصيد الإجازات | `data/leave_repo.dart` + `screens/leave_balance_screen.dart` | **v57** |
| تذكير الأذكار | `core/adhkar_reminders.dart` + `screens/worship/adhkar_reminders_screen.dart` | — |
| الكبسولة الزمنية | `screens/time_machine_screen.dart` | — |
| قواعدى (لو … يبقى …) | `core/custom_rules.dart` + `data/rules_repo.dart` + `screens/rules_screen.dart` | **v58** |
| سعر الدولار | `core/fx_rate.dart` + `screens/money/fx_rate_screen.dart` | — |

- **سعر الدولار** بيستخدم **Frankfurter** (مجانى، بدون مفتاح، read-only) +
  كاش يومى — ملتزم بالقرار الحاكم «لا سيرفر لا تكلفة».
- **قواعدى** = محرك قواعد محلى بحت (`ruleFires` + `metricValues`)؛ مفيش أى نداء
  خارجى.

### ب) اتشال من السايدبار (`lib/screens/app_drawer.dart`)

**موجة ١ — بنود كاملة اتشالت:** اسأل مديرك · البيت · قائمة التسوق · السيارة ·
التجديدات · السفر · (ورصيد الإجازات اتشال بعدها).

**موجة ٢ — إعادة تسمية:** الصلاة والأذكار → **صلاتى** · الفلوس → **فلوسى** ·
ملابس → **ملابسى**. وبعدين **تبديل ترتيب** فلوسى ↔ صحتى.

**موجة ٣ (آخر حاجة، commit `23f6e38`) — بنود فرعية:**
- **تذكيراتى:** «المناسبات» — ودى **مكانتش بند سايدبار أصلاً**، كانت **تبويب**
  جوه شاشة المواعيد. فاتشال التبويب، وبما إن الشاشة بقت تبويب واحد اتشال
  الـ`DefaultTabController`/`TabBar`/`TabBarView` خالص + كلاس `_OccasionsTab`
  + ٣ امبورتات (`occasions_repo` · `occasion_form` · `contacts_import`).
  **`occasions_repo` نفسه لسه شغال فى شاشة اليوم** — الجدول والبيانات ما اتلمسوش.
- **فلوسى:** أظرف المرتب · سعر الدولار · أموالى الخارجية · الواجبات الاجتماعية
  → بقت **٦ كروت**: الجمعيات · الديون والسلف · الادخار · المحفظة ·
  قائمة الأمنيات · الاشتراكات.
- **تطوّرى:** مفكرة الامتنان · أرقامك القياسية · اليوم المثالى · قائمة المشاهدة ·
  مراجعة القرآن · الكبسولة الزمنية · الخزنة السرية → بقت **٩ كروت**:
  تحليلات العادات · القراءة · التعلّم · الأهداف · صلة الرحم · عدّاد الإقلاع ·
  اليوميات · التحديات · كلمات السر.
- **+ ١١ امبورت** بقوا مش مستخدمين اتشالوا (شيل بند = شيل امبورته، وإلا
  `unused_import` بيكسر شرط الـ0 issues).

**السايدبار النهائى (اتأكد على الجهاز):** الرئيسية · تذكيراتى · ─ · صلاتى ·
صحتى · فلوسى (بادچ) · ملابسى · تطوّرى · المتابعة والأدوات · ─ · كارت الطوارئ ·
(فاتح / English / الإعدادات) · تسجيل الخروج.

### ج) ✅ اتقفل — الشاشات المشالة اتمسحت نهائيًا (commit `9d8c485`، DB **v59**)

المستخدم اختار **الخيار الأقصى**: امسح الكود **والجداول والبيانات**. اتعرض
عليه صراحةً إن ده **مايترجعش فيه** (الخزنة السرية/الامتنان/المشاهدة/العدادات
فيهم كتابته الحقيقية) واختار المسح.

**اتمسح: ١٨ شاشة + ١٨ repo/core + ١٥ كلاس موديل + ١٦ جدول** (−٧٠٧٦ سطر).
نقطة رجوع: tag **`before-purge-18-screens`**.

- **الـ١٨:** رصيد الإجازات · أظرف المرتب · سعر الدولار · أموالى الخارجية ·
  مفكرة الامتنان · أرقامك القياسية · اليوم المثالى · مراجعة القرآن ·
  الكبسولة الزمنية · الخزنة السرية · العدادات · السيارة · التجديدات · السفر ·
  الواجبات الاجتماعية · قائمة المشاهدة · جرد البيت · الضمانات.
- **DB v59** = `DROP TABLE IF EXISTS` لـ١٦ جدول (`_v59DroppedTables`) + شيل
  الـDDL من `createSchema`. الأبناء الأول (`trip_items` قبل `trips`).
- **مش مشال (لسه موصول من مكان تانى — اتأكدنا بـgrep قبل الحذف):**
  «اسأل مديرك» (شريط الرئيسية) · «صيدلية البيت» (صحتى) · «صيانة البيت» +
  «النباتات» (الفلوس/التنبيهات) · «قائمة التسوق» (٦ أماكن) · «كلمات السر» ·
  جدول `memorization` · جداول المصحف.

> **الدرس المنهجى:** «شِيل من السايدبار» **≠** «كود ميت». قبل أى حذف، اعمل
> `grep` على اسم الكلاس عبر `lib/` كله — طلع إن ٥ بنود لسه موصولة من الرئيسية
> أو من هَب تانى، و٧ بنود كانت موصولة من **البحث الشامل** بس. وأخطر حتة:
> **مراجع الجداول فى الكود المشترك** (`day_log_repo._dayTables` ·
> `search_repo` · `attention` · `dashboard_stats` · `week_overview` ·
> `local_brain` · `year_review` · `seed_demo` · `main.dart`) — `flutter
> analyze` بيمسك المراجع الميتة بس، **مابيمسكش استعلام SQL على جدول اتمسح**؛
> دى بتنفجر وقت التشغيل. اتحققنا بتشغيل البحث + تقويم النتيجة على الجهاز.

### د) الجارى / المؤجّل

1. **تغيير اسم التطبيق** — لسه مفتوح؛ المستخدم بيختار اسم. لما يستقر: يتغيّر فى
   اللابل (Android) + الأونبوردنج + العنوان عربى/إنجليزى.
2. **باقى بنك الأفكار** (`ROADMAP_ESTIMATES.md`) — لكن **بشرط** قرار (أ) فوق:
   ما ينفعش نضيف بنود سايدبار جديدة من غير ما نسأل.
3. **تجربة ميدانية بشرية** على الجهاز — البنود الـ٩ اتبنوا واتأكدنا إنهم بيفتحوا،
   بس مااتجربوش بإدخال بيانات حقيقية على مدى أيام.

### هـ) جوتشا اتكشفت الجلسة دى

- **`sed` بيعدّ سطور الإدخال:** حذف كذا سطر بـ`sed -i '17d;19d;…'` آمن — الأرقام
  بتشاور على ترقيم الملف الأصلى، مش بتزحلق مع الحذف.
- **الـheredoc:** أداة الـBash هنا **Git Bash مش PowerShell** — الـhere-string
  بتاع PowerShell (`@'…'@`) بيدخل `@` جوه رسالة الـcommit. استخدم
  `git commit -F - <<'MSG'`.
- **جهازين على نفس الموبايل بيتلخبطوا:** `com.hhub.prisons_inspector` (تطبيق تانى
  خالص) كان بيطلع قدام لما أدوس Back من شاشة my_assistant الرئيسية، فالسكرين شوت
  بيطلع من التطبيق الغلط. الحل: `adb shell am force-stop` **للاتنين** قبل ما
  تشغّل، وخد سكرين شوت تتأكد إنك فى التطبيق الصح قبل أى دوسة.
- **متستخدمش زرار Back للتنقّل جوه التطبيق فى التحقق** — من الشاشة الرئيسية
  بيخرّج من التطبيق. استخدم سهم الرجوع بتاع الـ`AppBar` أو أعد التشغيل.

---

## جلسة (2026-07-16/17) — خطة «مفيد لكامل اليوم»: المراحل الأربعة اتنفذت

المستخدم اختار **المراحل الأربعة كلها** من الخطة (`DEVELOPMENT_PLAN.md` — خطة ٢).
اتنفذوا بالترتيب، كل مرحلة بدورة كاملة (analyze → test → APK → تثبيت على
`RFCW70PZYSZ` → web → commit). **DB v52 · 282 تست** (منهم ٣ تستات ودجت جداد).

**مرحلة ١ — قوس اليوم** (commit `20bc41a`):
- «قفل اليوم» 🌙 (`core/day_close.dart` + `screens/day_close_screen.dart`) — مراجعة
  مسائية بتلف على الناقص (صلوات/مياه/عادات/جرعات) وبتسجّله بضغطة؛ شريط على
  الرئيسية بيظهر من ٦ مساءً.
- موجز صباحى صوتى (`core/morning_brief.dart` + زر 🔊 جنب شريط المدير، TTS).
- وجبات مفضلة (`MealsRepo.frequentMeals/lastMeal` + شرائح ⭐ + «سجّل تانى»).
- تعديل يوم فائت اتعمّم: عادات + مياه (الصلاة كانت اتعملت قبلها).

**مرحلة ٢ — تعميق البنود** (commit `733da9f`, DB **v52**):
- `subtasks` + `focus_sessions` + `tasks.repeat_rule` + `habits.target_per_day` +
  `habit_logs.count`.
- مهام: تشيك-ليست فرعية · تكرار يومى/أسبوعى/شهرى (المتكررة **بتترحّل** لموعدها
  الجاى بدل ما تتقفل، و`nextOccurrence` بيلحق المواعيد الفايتة، والفرعية بترجع
  فاضية) · جلسة تركيز 🍅 (`screens/tasks/focus_screen.dart`).
- عادات معدودة (N مرة/يوم): عدّاد −/+، واليوم بيتحسب فى السلسلة **لما يوصل
  للهدف بس**.
- الفلوس: كارت مقارنة بالشهر اللى فات + شريط اتجاه ٦ شهور.
- الصلاة: كارت إحصائية شهرية (عدّ كل صلاة + أيام كاملة + نسبة التزام).
- ميزان السعرات (`core/kcal_balance.dart`): متوسط الأكل **المتسجّل بس** مقابل
  الهدف = عجز/فائض + معدل الوزن الفعلى كجم/أسبوع من القياسات.

**مرحلة ٣ — ذكاء محلى** (commit `a8a3c67`):
- العقل بيجاوب: «تحاليلي» · «آخر سكر صائم كام؟» (مطابقة اسم التحليل المخزّن +
  الاتجاه عن القياس اللى قبله) · «التطعيمات» · «الكشرى فيه كام سعرة؟» (من
  الـ٧٣ طبق، أرقام USDA محسوبة — مكوّن ناقص = **مفيش إجابة**، مش تقدير).
- `core/contextual_tips.dart`: فواتير مستحقة + تطعيم قريب + مناسبة + تحاليل خارج
  النطاق → بتتحقن فى الموجز الصباحى (بعد شيل الإيموجى للـTTS) وفى بريفينج العقل.

**مرحلة ٤ — بنية وأداء:**
- **فتح الرئيسية أسرع**: مزامنة Health Connect (أبطأ جزء — قنوات نظام) اتنقلت من
  `_load` لـ`_syncHealth` بتشتغل **بعد أول رسمة** وبتحدّث بـsetState خاص؛ زمن
  الفتح بيتقاس وبيتطبع (`debugPrint('فتح الرئيسية: Xms')`).
  **مقيس على الجهاز فعلًا: `فتح الرئيسية: 245ms`** (release, `RFCW70PZYSZ`).
  > **⚠️ جوتشا:** `dev.log` (من `dart:developer`) **مالوش أى أثر فى بناء
  > الـrelease** — بيروح للـVM service اللى مش موجود أصلاً، فمايظهرش فى logcat.
  > أول قياس طلع فاضى بسببه. **للقياس على الجهاز استخدم `debugPrint`** (بيعدّى
  > على `print` → `I/flutter` فى logcat حتى فى الـrelease).

**تنظيف اللوج — كل `dev.log` اتشال (٥٠ موقع / ٢٣ ملف):**
- **المشكلة الأكبر مش القياس:** ٤٥ من الـ٥٠ كانوا **تسجيل أخطاء** فى `catch`
  → يعنى **كل فشل صامت فى التطبيق كان غير مرئى تمامًا فى الإصدار**. وأسوأ:
  **دورة التحقق بتاعتنا كانت فيها ثغرة** — `grep "E/flutter"` بيمسك الأخطاء
  **غير الملتقطة** بس، فالفشل اللى بنمسكه بـ`catch` **مستحيل يظهر** فيها.
  كل «logcat نضيف» فى الجلسات اللى فاتت كان ممكن يخبّى فشل صامت.
- **الحل: `core/log.dart`** — `logError(what, e, [st])` بعلامة **`❌`** +
  `logInfo(msg)` بعلامة **`ℹ️`**. دالة واحدة = مكان واحد نغيّره بعدين (لو
  عايزين ملف لوج على الجهاز) + علامة ثابتة للـgrep:
  `adb logcat -d | grep "❌"`.
- **سطرين اتعالجوا مخصوص (مش تحويل أعمى):** `gemini.dart` كان بيطبع
  `response.body` كامل = رد Gemini اللى ممكن يحتوى السياق الصحى للمستخدم →
  بقى status code بس؛ و`money_screen` كان فيه `OCR receipt total: $total`
  (بقايا debug بمبلغ الفاتورة) → **اتشال**. **قاعدة مكتوبة فى `log.dart`:
  ماتحطش بيانات المستخدم فى اللوج — بيتقرا بـadb.**
- **متحقَّق:** تست بيعترض `debugPrint` ويتأكد من الصيغة + الجهاز أثبت
  `ℹ️ فتح الرئيسية: 310ms` فى logcat الريليز.

**ملف اللوج على الجهاز + «شارك التشخيص» (2026-07-17):**
- **`AppLog` جوه `core/log.dart`**: كل سطر بيتكتب فى
  `<AppSupport>/app.log` (`/data/user/0/com.hhub.my_assistant/files/app.log`)
  بختم وقت. **الهدف**: تشوف الأخطاء من غير ما توصّل الموبايل بكمبيوتر.
  محلى بالكامل — مافيش أى سيرفر (القرار الحاكم رقم ١).
- **التصميم مبنى على «اللوجر عمره ما يكسر التطبيق ولا يبطّئه»**: كل حاجة
  `try/catch` صامت · الكتابة **مجمّعة** (Timer ثانيتين، مش I/O لكل سطر) ·
  السطور قبل `init` بتتجمّع فى الذاكرة (بسقف ٢٠٠) وبتتكتب أول ما يجهز ·
  تدوير عند ١٢٨ ك.ب (بنسيب آخر نصه — الأحدث هو المهم) · `kIsWeb` = مفيش ملف.
- **`screens/diagnostics_screen.dart`** (إعدادات ← النسخ الاحتياطى ← «شارك
  التشخيص»): **بيعرض اللوج قبل المشاركة عن قصد** — التطبيق مبنى على إن
  بياناتك ماتخرجش، فلازم تشوف بعينك بتبعت إيه. + نسخ + مسح.
- **`AppLog.init()` بينادى من `main` الأول** + بيطبع `ℹ️ ملف اللوج: <path>
  (N bytes)` — ده **الدليل الوحيد** إن الملف شغال فى الريليز، لأن `run-as`
  مابيشتغلش على نسخة مش debuggable فمفيش طريقة تقرا الملف من بره.
- **التحقق على الجهاز:** تشغيلة سجّلت سطور → قفل → تشغيلة تانية قرأت
  **`app.log (196 bytes)`** = الكتابة عاشت على القرص فعلًا.
  > **⚠️ فخ فى الاختبار (وقعت فيه):** `adb shell pm clear` قبل الاختبار
  > بيرمى التطبيق على **شاشة الترحيب** — فالرئيسية ماتتحملش، مفيش `logInfo`
  > بيتنادى، والملف عمره ما بيتعمل → «٠ بايت» اللى شكله باج **هو نتيجة
  > الاختبار الغلط مش الكود**. اختبر الاستمرارية من تشغيلة عادية (من غير
  > `pm clear`).
  > **⚠️ وفخ تانى:** أول تست كان بينادى `flushForTests()` مباشرة → **مسار
  > الـTimer الحقيقى ماكانش متجرب أصلاً**. فيه دلوقتى تست بيسجّل سطر ويستنى
  > ٣ ثوانى من غير أى نداء يدوى.
- **⚠️ جوتشا تانية (من غلطة حصلت فعلًا):** المسح الأول اتعمل بـregex فيه
  `re.DOTALL` + `.*?` → النمط بدأ عند `dev.log(` فى سطر ٥٩ وفضل يمدّ **عبر
  ١٥ سطر** لحد ما لقى `, error: e)` فى سطر ٧٤، فحطّ أول الاستبدال فوق وآخره
  تحت وخرّب الملفات. **أى مسح على كود لازم يبقى سطر-بسطر (بدون DOTALL)**،
  واللى مش على سطر واحد يتساب ويتظبط بالإيد. الرجوع كان `git checkout -- lib/`.
- ودجت الشاشة الرئيسية بقى فيه زرارين كمان: 🍽 وجبة · ☑ المهام (بيفتحوا التطبيق
  عبر `myassistant://open/...` → `_handleWidgetUri` فى `shell.dart`).
- **`test/widgets_test.dart`** (جديد): ٣ تستات بترسم الشاشات فعلًا (جلسة التركيز
  · قفل اليوم · عدّاد العادة المعدودة).

> **⚠️ درس مهم (اتكشف بالتست):** فى `testWidgets` لازم
> **`databaseFactoryFfiNoIsolate`** مش `databaseFactoryFfi` — الأخير بيشتغل فى
> isolate منفصل، والـfuture بتاعه عمره ما بيخلص جوه الزمن الوهمى بتاع
> `testWidgets`، فالشاشة بتفضل على `CircularProgressIndicator` و`pumpAndSettle`
> بيقعد **١٠ دقايق** لحد ما يعمل timeout (والتست اللى بعده بيقع بـ«Guarded
> function conflict»). `repos_test.dart` عادى بيستخدم `databaseFactoryFfi`.
> **ودرس تانى:** `arNum` بيرجّع أرقام **لاتينية** (قرار 2026-07-09 مكتوب فى
> `core/ar.dart`) — أى تست بيتوقع «٠/٣» غلط، الصح «0/3».

---

## ★★ قرار حاكم (2026-07-15) — لا سيرفر، لا تكلفة، الأرقام من مصدر مرجعى

**دول قاعدتين فوق أى اقتراح جاى — لو اقتراح بيخالفهم، مايتعرضش أصلاً:**

1. **كل حاجة محلية ومجانية.** المستخدم بالنص: «عايز كل البيانات و الصور تخزين
   محلى **مش عايز حاجه تتخزن على سيرفر نهائى** عايز التطبيق مجانى بالكامل **مش
   عايز تكلفه نهائى**». عليه: **النسخة الاحتياطية السحابية المشفّرة (Google
   Drive) اترفضت** رغم إنها كانت مجانية. المسموح: SQLite محلى + ملفات على
   الجهاز + APIs مجانية بدون مفتاح للقراءة بس (open-meteo · Open Food Facts ·
   everyayah · صور المصحف).
2. **ممنوع كتابة رقم غذائى (أو أى رقم مرجعى) بالإيد.** المستخدم: «عايز الارقام
   تبقى مظبوطه **يا اما مش هيبقى ليه فايده**». الأرقام تيجى من مصدر موثوق
   وتتحقّق. ترجمة **اسم** = آمنة؛ اختراع **رقم** = ممنوع. العنصر اللى المصدر
   ماعندهوش قيمة ليه = **null مش صفر**.

---

## ★ أحدث جلسة (2026-07-15/16) — دليل الأكل USDA + إعادة بناء الرئيسية

**الحالة:** DB **v50** · **256 تست** · analyze نضيف · مثبّت ومتحقَّق على
`RFCW70PZYSZ` · web مدفوع · آخر commit `ae7b8f9`.

### خلص — قرارات منفّذة (بالترتيب)

**أ) التحسينات الـ٣ المختارة (من قايمة سابقة):**
1. **وضع ليلي مجدول** — `AppState.scheduleEnabled/darkFrom/darkTo` +
   `isDarkWindow` نقية (بتتعامل مع تجاوز منتصف الليل) + `Timer.periodic` كل
   دقيقة. اختيار يدوى للثيم **بيوقف الجدولة** (المستخدم يكسب).
2. **ويدجت أوسع** — 5×3، عمودين «المهام | الفواتير».
3. **تصدير كل البيانات** — `core/data_export.dart`: CSV لكل جدول (BOM للعربى)
   → zip → مشاركة. **بيستبعد جدول `passwords`** عمداً.

**ب) باقى خطة التطوير (٣ دفعات):**
4. قوالب مواعيد + **رؤية استباقية يومية** (`core/proactive_insight.dart`، إشعار
   ٨:٣٠ بأقوى رؤية من `buildInsights`).
5. **DB v49**: سجل التطعيمات + عمود `location` على المواعيد + فتح الخرائط.
6. **مخطّط سداد الديون** (كرة الثلج) + **مشاركة الختمة كصورة** (RepaintBoundary).
7. **DB v50**: **مؤشرات التحاليل** — منحنى اتجاه + نطاق طبيعى + `outOfRangeCount`.
8. **تأجيل الإشعار** («⏰ أجّل ساعة») + **تقارير PDF عامة** (`SectionPdf`).
9. **جولة تعريفية** + **بحث موحّد ١٥→٣٠ بند** + شرائح فلترة.

**ج) دليل الأكل — أهم شغل الجلسة:**
10. القديم كان **١٠٤ صنف متكتوبين بالإيد** (٤ عناصر، مفيش طرق طهى) = بالظبط
    اللى المستخدم خايف منه. اتستبدل بـ**USDA FoodData Central SR Legacy**
    (ملكية عامة). **`tools/build_food_db.py`** بيبنى
    `assets/food/usda_foods.json` (١.٨ ميجا، **٥٩٩٠ صنف**، ١٢ عنصر، حصص منزلية).
11. **طرق الطهى** بتتقرا من وصف USDA → `prep` + تجميع `g` (نيّئ/مسلوق/مقلى...).
    حقيقى: صدور فراخ مسبّك ١٥١ · مشوى ١٦٥ · مقلى ١٨٧.
12. **التسمية العربية** بقاموس (كلمة رئيسية + جزء + طريقة) → **٨٥٪**.
13. **تنظيف حلال (باختيار المستخدم)**: خنزير/خمور/قطبى (٧٤٣) + بيبرونى/سلامى/دم/
    جيلاتين/منفحة/كحول/خيل/ضفادع/شحم (٤٧) + **اللحوم النيّئة (٨٣٩)**.
    **الخضار والفاكهة النيّئة (٣٩١) فضلت** — دى بتتاكل نيّئة فعلاً.
14. **الربط بتسجيل الوجبات**: `UsdaFood.toFoodItem()` — الأرقام بتتنقل زى ما هى.
    المصادر **مسمّاة بصراحة**: «✓ أرقام رسمية (USDA)» ثم «أكلات مصرية (أرقام
    تقديرية)» ثم «منتجات من الإنترنت».

**د) الرئيسية:**
15. **شريط «محتاج منك دلوقتي»** (`core/attention.dart`) فوق خالص + **حلقات
    «يومك فى سطر»** + **إخفاء الأقسام الفاضية** + **سحب وترتيب**.
16. **صف الإجراءات**: «اسأل مديرك» بعرض الشاشة فوق · **➕ أول اليمين** ·
    الفلوس · مواعيد (بادج) · مهام (بادج) · صوت · **شريط طوارئ رفيع بعرض الشاشة**.
    الـ١٢ زرار اتلمّوا جوه الـ➕ (شيت ٣ فى الصف، ٩٥٪ من الشاشة).
17. **شيل «التمرين» و«فلوس النهارده»** + **~١١٠ سطر كود ميت** كشفهم الشيل.
18. **كروت الأقسام على الرئيسية** (`core/dashboard_stats.dart` +
    `widgets/dash_card.dart` مشتركة) — ١٢ قسم بأرقام حية، قسم «أقسامك»
    بيتخفى/يترتّب زى أى قسم + صفحة «لوحة شاملة» منفصلة للعرض الكامل.

### قرارات ثابتة (لا تُنقَض)

- **المعمار**: أى تجميع بيانات يتعمل فى **`core/` من غير ودجت** عشان يتختبر
  (`collectAttention` · `collectDashboard` · `UsdaDb`)، والشاشة بتربط المفتاح
  بالأيقونة/اللون/الوجهة بس. اتّبع النمط ده فى أى تجميع جديد.
- **مفيش نص مقصوص بأى لغة**: `FittedBox(scaleDown)` **مش كفاية لوحده** —
  بيمنع القص بس بيطلّع خط مايتقراش. لازم أسماء قصيرة فى الصف + سطرين فى الشيت.
  **فيه تستات بتقيس كل نص بـTextPainter فى اللغتين وبتفشل تحت ٨٠٪** — سيبها.
- **الأقسام الفاضية مالهاش كروت/بنود** — لوحة مليانة أصفار مالهاش قيمة.
- **كل كارت معزول** (try/catch لكل قسم) — قسم بايظ مايكسرش اللوحة.

### الجاى (للاستكمال) — مرتّب بالأولوية

1. ~~**الأكلات المصرية أرقامها تقديرية**~~ — **✅ خلص (2026-07-16)**:
   `core/egyptian_dishes.dart` — كل أكلة وصفة (fdcId + جرامات)، والسعرات
   **تتحسب** من أرقام USDA (`dishNutrients`)؛ null لو مكوّن مفقود. ١١ أكلة،
   الـids اتأكّدت يدوى. بتظهر فى «دليل الأكل» تحت «أكلات مصرية (محسوبة من
   USDA)» بكارت فيه تفصيل المكوّنات. **ممكن نزوّد أكلات (محشى/رقاق/بامية/بط...)
   بنفس الطريقة لو طلب.**
2. **«لوحة شاملة» المنفصلة** — لسه موجودة مع كروت الرئيسية. لو شافها تكرار نشيلها.
3. ~~الـ٤ أزرار جنب الـ➕ مش قابلة للتخصيص~~ — **✅ خلص (2026-07-16)**: كتالوج
   ٩ اختصارات + المستخدم بيختار ٤ ويرتّبهم عبر `QuickActionsSettingsScreen`
   (إعادة استخدام)، محفوظ فى `home_shortcuts`. زرار «تخصيص الاختصارات» تحت الصف.

**ملاحظة:** كل البنود المفتوحة القابلة للتنفيذ خلصت. فضل بس مراجعة «لوحة شاملة»
المنفصلة (بند ٢) — قرار للمستخدم مش شغل. + توسيع دليل الأكل (اختيارى): الأطباق
الجاهزة بقت **٧٣** (٤٢ مصرى + ٣١ عالمى) — ممكن نزوّد أكتر لو طلب.

### مرفوض / مؤجّل (وأسبابه)

| البند | السبب |
|---|---|
| نسخة سحابية مشفّرة (Drive) | **مرفوض** — القرار الحاكم: مفيش سيرفر |
| قراءة SMS البنك | محتاج `READ_SMS`، Play بيقيّده (السايد-لود بيتخطاه) — المستخدم أجّله |
| مزامنة تقويم ثنائية | بلوكر: `device_calendar` بيتعارض مع `timezone 0.10` |
| قبلة بالكاميرا AR | معقّد — بوصلة الإبرة كافية |
| الأربعون النووية | مفيش مصدر عربى حر نظيف |
| عملات متعددة | استخدام نادر |

### جوتشا اتكشفت (مهمة لأى جلسة جاية)

- **`createSchema` بيبنى آخر نسخة دايماً** مهما كان الـ`version` — يعنى
  **ماينفعش** `createSchema` بعده `upgradeSchema` (الجدول موجود). التست الصح:
  قاعدة **فاضية** + `upgradeSchema(N-1, N)`؛ ولو الفرع بيعمل ALTER لجدول
  موجود، ابنى شكله **القديم** بالإيد الأول (شوف تست v48→v49).
- **الـisolate الخلفى للإشعارات**: `Notifications._ready` = false هناك، فلازم
  `await Notifications.init()` جوه الهاندلر قبل أى جدولة وإلا بتفشل بالسكوت.
- **الفلاتر النصية والإيجابيات الكاذبة**: `Beans, black turtle` فاصوليا مش
  سلاحف · `KLONDIKE SLIM-A-BEAR` أيس كريم مش دب · `Turkey Pepperoni` حلال ·
  `Gelatins` بالجمع بتفلت من `\bgelatin\b`. **افحص قبل ما تمسح.**
- **شبكة جوه ListView** لازم `shrinkWrap: true` + `NeverScrollableScrollPhysics`.
- كونسول ويندوز `cp1252` مابيطبعش عربى — اكتب النتيجة فى ملف بدل `print`.
- الجهاز `RFCW70PZYSZ` **بيفصل من USB كتير** — لو التثبيت فشل استنى وأعد.

### مفاتيح الاستئناف

- إعادة بناء قاعدة الأكل: `python tools/build_food_db.py` (محتاج `usda.zip`
  من `https://fdc.nal.usda.gov/fdc-datasets/FoodData_Central_sr_legacy_food_csv_2018-04.zip`)
  → ينسخ `usda_foods.json` لـ`assets/food/`. **وبعدها أعِد التحقق من الأرقام**
  مقابل `https://api.nal.usda.gov/fdc/v1/food/<id>?api_key=DEMO_KEY`
  (DEMO_KEY محدود ٥٠/يوم = للتحقق بالعيّنة بس).
- notif ids: الفاضى بعد **~1200000** (آخر مستخدَم: تطعيمات 1190000+id).
- الترتيب المعتاد: analyze → test → build apk → adb install + pidof + logcat →
  build web → commit.

---

## جلسة (2026-07-14) — تنفيذ خطة التطوير الشاملة (سايدبار + ٩ بنود جديدة + تطوير)

> **الحالة:** DB **v42** · **205/205 اختبار** · analyze نضيف · مثبّت على `RFCW70PZYSZ` + منشور Pages · آخر commit `2e181d7`.
> **الخطة الكاملة:** `DEVELOPMENT_PLAN.md`. **المبدأ:** كل حاجة **مجانية/محلية** (بدون سيرفر)؛ البدائل المدفوعة ليها بديل مجاني (AI=عقل محلي/Gemini-مفتاح-مجاني/LLM-محلي · نسخ احتياطي=درايف المستخدم · خرائط=OpenStreetMap).

### خلص — قرارات منفّذة (بالترتيب)
**(أ) إعادة هيكلة السايدبار** — فصل «الصحة واللياقة» لـ٣ (**الصحة/الرياضة/النظام الغذائي**) · فك دمج «البيت والمشتريات» ثم عمل مجموعة **«البيت»** (صيانة/ضمانات/عدادات/نباتات) + **«ملابس»** بند مستقل + «قائمة التسوق» مستقلة · **«الصلاة والأذكار» فوق «الفلوس»** مباشرة · شيل **«المصحف»** من السايدبار (جوّه الصلاة) · شيل أسهم المجموعات · **«اليوم»→«الرئيسية»** و**«الجدول»→«المواعيد»** · توحيد خط كل البنود (١٥/w500) · **«تسجيل الخروج»** في الأسفل (يقفل التطبيق) · نقل **«الأدوية»** من المواعيد لمجموعة الصحة (`MedsScreen` جديدة تلفّ `_MedsTab`) · **«حياتي وتطوّري»→«تطوّري»**.
**(ب) التقويمات + منتقي التاريخ** — `widgets/history_calendar.dart` (تقويم شهري عام: نقاط نشاط + شيت ملخّص اليوم) موصّل للفلوس (`MoneyRepo.dayReport`) والصحة (`HealthRepo.dayReport`) + تقويم العبادات الموجود · `widgets/month_year_wheel.dart` (عجلة `CupertinoPicker` للشهر/السنة تفتح بالضغط على العنوان) · عكس اتجاه أسهم التقاويم.
**(ج) كروت قابلة للترتيب** (`widgets/reorderable_cards.dart`، سحب بالضغط المطوّل، ترتيب محفوظ لكل صفحة) على كل هَبّات المجموعات + أدوات الصلاة.
**(د) المصحف** — تعليم الآية الجارية أثناء التلاوة في شيت التفسير (تمرير تلقائي + `DraggableScrollableController`)؛ **الصندوق على الصورة نفسها متأجّل** (محتاج بيانات إحداثيات مصحف المدينة، مفيش مصدر مجاني موثوق).
**(هـ) ٩ بنود جديدة** — DB **v39** «المهام والمشاريع» (`tasks_repo`) + «الاشتراكات» (`subscriptions_repo`، إجمالي شهري) + «الأهداف» بمعالم (`goals_repo`) · DB **v40** «السيارة» (`cars_repo`، كفاءة وقود كم/لتر) + «التجديدات» (`renewals_repo`، تنبيه قبل الانتهاء) · DB **v41** «السفر» (`trips_repo`) + «التعلّم» (`courses_repo`) + «الحيوانات» (`pets_repo`) + «كلمات السر» (`passwords_repo`، محمي بالبصمة).
**(و) تطوير بنود موجودة** — **الفلوس**: ميزانية لكل فئة (`settings.categoryBudgets`) + شريط تخطّي أحمر + **تصدير Excel/CSV** (`core/money_export.dart`، UTF-8 BOM) · **الرياضة**: مؤقّت راحة (`screens/gym/rest_timer.dart`، اهتزاز عند الانتهاء) — تسجيل الجلسة والـPR كانوا موجودين · **الصحة** DB **v42**: مفكرة الأعراض (`symptoms_repo` + شاشة) + التزام الدواء (`MedsRepo.adherencePercent`) + قسم الأعراض في تقرير الدكتور.

**(ز) النظام الغذائي** — DB **v43**: الصيام المتقطّع (`fasting_repo`، مؤقّت حيّ + تنبيه اكتمال الهدف + `rescheduleCurrent` عند الفتح) + مخطّط وجبات أسبوعي (`meal_plan_repo` + شاشة، «أضف الكل لقائمة التسوق») · **مسح باركود الأطعمة** (`mobile_scanner ^5.2.3` + إذن CAMERA؛ `food_db.lookupBarcode` → Open Food Facts مجاني؛ `barcode_scan_screen` كاميرا + إدخال يدوي، محمي `kIsWeb`؛ زر 📷 في `food_picker_sheet`). **APK كبر 58→71MB** (نماذج ML Kit للباركود). **208 اختبار.**

### الجاري
لا يوجد شغل نص-منفّذ — كل الدفعات اتعملها commit واتأكّدت على الجهاز. **البند التالي في الطابور = الرئيسية (نظرة الأسبوع + ملخّص صباح).**

### المؤجّل / الجاي (نكمّل منه) — كله مجاني
- **النظام الغذائي:** مسح باركود الأطعمة (`Open Food Facts` مجاني) + مخطّط وجبات أسبوعي + صيام متقطّع.
- **الرئيسية:** نظرة الأسبوع + ملخّص صباح ذكي + ويدجت أوسع.
- **اسأل مديرك:** توسيع العقل المحلي + مدخل/قراءة صوتية + رؤى استباقية.
- **المواعيد:** عرض شهري (تقويم) + مزامنة ثنائية مع تقويم الموبايل.
- **الصلاة:** بوصلة قبلة بالكاميرا + اختيار طريقة الحساب + مشاركة الختمة.
- **الفلوس:** قراءة `SMS` البنك → مصروف تلقائي (**محتاج إذن `READ_SMS` الحسّاس — آخر حاجة**).
- **البيت:** جرد ممتلكات + رسم استهلاك العدادات + تقدير الفاتورة.
- **ملابس:** مخطّط إطلالات + تتبّع غسيل. **التسوق:** تصنيفات + تتبّع أسعار + أساسيات متكررة.
- **تطوّري:** تتبّع قراءة + مفكرة امتنان + تحليلات عادات.
- **المتابعة:** مراجعة سنوية + تصدير PDF + نسخ احتياطي سحابي مشفّر (درايف المستخدم).
- **شامل:** ويدجتات أكتر · `Wear OS` · `LLM` محلي للدردشة.

### مفاتيح الاستئناف
- **DB الحالي v42؛ الهجرة الجاية = v43.** نطاقات الإشعارات: task `1110000` · subscription `1120000` · carEvent `1130000` · renewal `1140000` · petEvent `1150000` · trip `1160000` · **التالي المتاح `1170000+`**.
- **حلقة التحقق لكل دفعة:** `flutter analyze` → `flutter test` → build apk arm64 → `adb install -r` + monkey + `pidof` + logcat (تأكيد الهجرة نضيفة على قاعدة الجهاز) → build web → commit + push.
- قالب أي بند جديد: جدول(جداول) في `db.dart` (bump version + `_vNNTables` + فرع الترقية + حلقة `createSchema`) → موديل في `models.dart` → repo في `data/` → شاشة في `screens/` → توصيل في `app_drawer.dart` → تنبيهات في `main.dart` (`rescheduleAll`) → اختبار الهجرة + الـrepo.

---

## جلسة (2026-07-12/13) — بند «الصلاة والإسلام»: منظومة إسلامية كاملة

> **الحالة:** DB **v37** · **186/186 اختبار** · analyze نضيف · مثبّت على `RFCW70PZYSZ` + منشور Pages.
> **حزم جديدة:** `flutter_compass` · `scrollable_positioned_list` · `audioplayers` · `cached_network_image` · `flutter_cache_manager`.
> **آخر commit:** `35ea471`. **البند كله في `views/worship/` + hub «الصلاة والأذكار».**

### خلص (قرارات منفّذة) — بالترتيب
1. **مسمّى النوع** راجل/سيدة → **ذكر/أنثى** (Man/Woman→Male/Female) — عرض فقط، القيمة `male/female` ثابتة، بند الدورة الشهرية للإناث ما اتأثّرش.
2. **كارت الصلاة مصغّر** في الرئيسية (الصلاة القادمة + عدّاد) → يفتح **صفحة «الصلاة والأذكار»** (hub) + بند في السايدبار.
3. **بوصلة القبلة** (`flutter_compass`، اتجاه محسوب للكعبة، حماية `kIsWeb`) + **المسبحة** + **أذكار الصباح/المساء** + **أسماء الله الحسنى (بالمعاني + وضع حفظ)** + **أدعية مأثورة مصنّفة** + **دعاء/آية/حديث اليوم**.
4. **ختمة القرآن** (ورد يومي + كام يوم تخلّص) + **مواقيت الشهر** (جدول + PDF) + **تتبّع السنن** + **تتبّع الصلوات الخمس (سلسلة 🔥)**.
5. **صوت أذان**: أزيلت الأصوات المدمجة → **كل مستخدم يرفع ملفه من جهازه** (FileProvider → content URI + grant لـ systemui)؛ غير كده تنبيه منبّه قوي بخصائص alarm. زر «جرّب الصوت».
6. **تذكيرات**: الجمعة (سورة الكهف) · السنن الرواتب بعد كل فرض · السحور/الإفطار.
7. **سلسلة الأذكار** + **تتبّع الصيام** (اثنين/خميس/الأيام البيض بالهجري) + **إحصائية روحية أسبوعية**.
8. **حاسبة الزكاة** (نصاب 85جم، 2.5%) + **حاسبة المواريث** (فروض + تعصيب 2:1 + عَوْل + رَدّ + العمريّتان) + **أذكار بعد الصلاة** (عدّاد موجّه) + **المناسبات الإسلامية** (عدّ تنازلي هجري).
9. **المصحف — النص**: `assets/quran/quran.json` (**Tanzil**، متحقَّق 114 سورة/6236 آية/عدّ حفص) + قارئ (تكبير خط/علامة آخر موضع/**تفسير الميسّر** عند الضغط/**تلاوة everyayah** بـ5 قرّاء + تشغيل السورة كاملة).
10. **المصحف — صور الصفحات**: 604 صفحة (مصحف المدينة، `android.quran.com`) **بث + تخزين تلقائي + تحميل كامل (~21MB)**؛ **سحب الصفحة لأعلى = تفسير آيات الصفحة + نبذة عن السورة** (مكية/مدنية + متى/أين نزلت + ترتيب النزول + الجزء + الصفحة + عدد الآيات).
11. **أذكار المواقف** (حصن المسلم مصغّر) + **الوِرد اليومي** (أهداف + عدّاد) + **الرقية الشرعية** (من المصحف المتحقَّق) + **دليل العمرة والحج** + **حاسبة العمر الهجري + عدّاد الرمضانات**.

### قرارات ثابتة (لا تُنقَض)
- **دقة النص الشرعي أولوية قصوى**: **مايتكتبش قرآن/حديث بالإيد إطلاقًا**. المصادر متحقَّق منها:
  القرآن = **Tanzil** (مصحف المدينة، CC BY 3.0) · التفسير = **الميسّر/مجمع الملك فهد** ·
  صور المصحف = صور **Quran.com** (مصحف المدينة) · التلاوة = **بث** من **everyayah** (قرّاء معتمدين).
- **حقوق الملكية**: تسجيلات المشايخ المشهورين محمية → **ما تتحطّش مدمجة في تطبيق منشور**.
  **بث ≠ تخزين** فالتلاوة بالبث مسموحة؛ الأذان = ملف المستخدم نفسه (مش مدمج).
- **التكلفة = $0 دائمًا**: كل النصوص أوفلاين جوّه التطبيق؛ الصوت/الصور/الطقس بث من خدمات
  **مجانية بدون مفتاح** + نت المستخدم + كاش محلي؛ **مفيش سيرفر بندفع له** (حتى لو مليون مستخدم).
- كل الأدوات في **grid صفحة «الصلاة والأذكار»** + بندين سايدبار (الصلاة والأذكار / المصحف).

### الجاري / المستقبلي (للاستكمال)
- **تحقق ميداني (المستخدم يؤكّد على جهازه):** تشغيل التلاوة الصوتية · صوت الأذان المخصّص (يعتمد على الجهاز/OEM) · ظهور صور المصحف + السحب للتفسير + النبذة.
- **الأربعون النووية** — مؤجّلة: مالقيتش مصدر JSON عربي حر نضيف (AhmedBaset 404 · jsdelivr 403). تُضاف لو ظهر مصدر موثوق (مش هتتكتب بالإيد).
- **نبذة السور — «الأهمية» كنص وصفي** — مؤجّلة: مالقيتش مصدر عربي حر (quran.com إنجليزي) → دلوقتي حقائق مؤكّدة بس. ممكن مقدّمات تفسير موثوقة لاحقًا.
- **أفكار مقترحة (لو حابب):** ودجت آية/حديث على شاشة الموبايل الرئيسية · تذكير أذكار الصباح/المساء في وقتها · مفضّلة للآيات (bookmark) · بحث في القرآن/التفسير · تنزيل تلاوة سور للأوفلاين.
- **DB history:** v34 `prayer_log` · v35 `sunnah_log`+`quran_khatma`+`khatma_reads` · v36 `dhikr_log`+`fasting_log` · v37 `wird_log`.

### جوتشا اتكشفت (مهمة لأي جلسة جاية)
- **قوائم `const List<int>` الطويلة (114/604): اكتبها سطر واحد + `// ignore: lines_longer_than_80_chars`** — التغليف اليدوي بيغلّط العدّ (طلعت 116 ثم 112 قبل ما تظبط 114؛ فيه تست بيتأكد من الطول).
- **قنوات إشعار أندرويد صوتها ثابت بعد الإنشاء** → قناة منفصلة لكل صوت/ملف (`prayer_adhan_<x>`).
- **`res/raw` دير جديد بيتجاهله merge** → محتاج `flutter clean` (اتحلّت لمّا اتشالت الأصوات المدمجة).
- **`lintVitalAnalyzeRelease` بيتعلّق على ويندوز (قفل AV على lint-cache)** → `lint { checkReleaseBuilds=false; abortOnError=false }` في app build.gradle.kts.
- **مصدر بيانات موثوق**: quran.com `/chapters` و`/info` بيرفضوا urllib (403) بس يشتغلوا بـ browser UA عبر curl.
- **INTERNET permission** اتضاف صراحةً للـmanifest (للبث في release).
- **audio/custom-adhan/mushaf-images** كلها device-dependent — التحقق النهائي على جهاز المستخدم.

---

## جلسة (2026-07-12) — الصحة واللياقة والتغذية + منظومة الموقع + إصلاح الويب

> **الحالة:** DB **v29** · **161/161 اختبار** · analyze نضيف · `geolocator ^13` جديدة ·
> مثبّت على `RFCW70PZYSZ` + منشور على GitHub Pages. **التفاصيل الكاملة في `SESSION_HANDOVER.md` (قسم 2026-07-12).**

### خلص (قرارات منفّذة)
1. إصلاحان UI: سهم السايدبار + شيل كارت المياه/النوم من الرئيسية (`91455a5`).
2. **قاعدة أكل بالسعرات والماكروز** (`core/food_db.dart` + Open Food Facts أونلاين) — DB v28 (ماكروز على `meals`) + `food_picker_sheet` + كارت ماكروز يومي (`c7600d2`).
3. **مكتبة تمارين** (`core/exercise_library.dart` + `exercise_library_screen`) بأجهزة/بدون (`6dca2c0`).
4. **أنظمة غذائية للاختيار** (`core/diet_plans.dart` + `diet_plans_screen`) بتظبط هدف السعرات والماكروز (`94e12d0`).
5. **تتبّع GPS مشي/جري** (`geolocator` + `location_tracker` + `walk_tracker_screen`) — DB v29 `activity_sessions`؛ Health Connect للخطوات موجود أصلًا (`d03b89e`).
6. **إصلاح كراش الويب**: نيتيف (`receive_sharing_intent`/`quick_actions`) اتحوّط بـ`kIsWeb` (`658b130`).
7. **منظومة الموقع دولة/مدينة/GPS** زي طارة: `core/countries.dart` (كل الدول) + `location_fields` + `reverseGeocode` + فلتر مدن بالدولة؛ الصلاة/الطقس بيتبعوا الموقع فورًا (`450128a`,`add26fe`).

### قرارات ثابتة
- أكل: قاعدة مدمجة أوفلاين + Open Food Facts أونلاين (مش هننزّل قاعدة ضخمة).
- موقع: وحدة على `customLocation(lat/lng)`؛ المحافظات fallback؛ GPS موبايل بس.
- الويب: عرض/تجربة فقط (لا إشعارات/مشاركة/GPS/ساعة) — أي نيتيف = `kIsWeb`-guard.
- APIs مجانية بدون مفتاح: Open-Meteo · Open Food Facts · BigDataCloud.

### الجاري / المستقبلي (للاستكمال)
- **ميداني على الجهاز**: تتبّع GPS فعلي، إذن الموقع، مزامنة Health، بحث الأكل أونلاين، تفعيل نظام غذائي.
- **تغذية**: باركود سكانر، شريط تقدّم ماكروز، وجبات متكررة/مفضلة، توسيع القاعدة.
- **تمارين**: صور/GIF، ربط تمارين المكتبة بجلسة الجيم، مؤقّت راحة.
- **أنظمة**: توليد خطة أسبوعية فعلية من النظام الغذائي؛ ربط النظام الرياضي بجدول التمرين.
- **موقع/GPS**: خريطة مسار (OSM)، wakelock أثناء التتبع، GPS على الويب (لو اتقرر).
- **مؤجّل**: iOS (Mac/Codemagic + Apple Dev)، Google Calendar (OAuth)، Gemini (مفتاح المستخدم).

---

## أحدث جلسة (2026-07-10) — نسخة ويب + عقل مجاني + إعادة هيكلة واجهة

> **الحالة النهائية:** DB **v27** · **145/145 اختبار** · analyze نضيف · مثبّت ومتحقّق على
> `RFCW70PZYSZ` (دفعات ١٢–٣٠). كل الشغل ده متعمله commits على `master`.

### أ) اللي خلص (قرارات منفّذة)
1. **DB v27 — صلاحية لكل كمية للأدوية** (`pharmacy_batches`): كل دفعة عدد + صلاحية مستقلة؛
   الصنف بيحسب إجمالي العدد + أقرب صلاحية. فورم الصيدلية بقى «مدير دفعات».
2. **نسخة ويب + نشر تلقائي على Firebase Hosting (زى طارة)** — *الكود جاهز ومتّجرب في المتصفح*:
   - `flutter build web` بيعدّي ويشتغل: `sqflite` على الويب عبر **`sqflite_common_ffi_web`**
     (IndexedDB + `sqlite3.wasm`)، فاكتوري بيتظبط من `db_init_web.dart` (conditional، ستَب على
     الموبايل)، الإشعارات/الودجت متحمية بـ `kIsWeb` في `main.dart`، **ML Kit OCR** conditional
     (`ocr_recognizer_native/stub`). `web/index.html`+`manifest.json` عربي RTL.
   - إعداد النشر: `firebase.json` + `.firebaserc` (placeholder) + `.github/workflows/deploy-web.yml`
     + `WEB_DEPLOY.md`. **بقى git repo** (أول commit).
3. **عقل المدير المجاني** (`core/local_brain.dart`) — **~٦٢ نوع سؤال، على الجهاز، بدون مفتاح**:
   فلوس/رصيد/محافظ · مصاريف + مقارنة شهرية · دخل/صافي · ديون (+بالاسم «مديون لأحمد») · صافي الثروة ·
   مركز مالي/صحي مجمّع · ادخار + توقّع الوصول · فواتير · أدوية + جرعات النهاردة · «عندي بانادول؟» ·
   عادات + أقوى/أضعف · صحة اليوم + جودة النوم + الساعة الذكية · قياسات + **اتجاه** («ضغطي اتحسن؟») ·
   مستندات + **بحث بالاسم** · صلاة (الجاية/الكل) · طقس · سعرات · الجمعية · مناسبات · مين أتصل بيه ·
   صيانة · نباتات · ضمانات · قرآن · إقلاع · عدادات · تحديات · الوارد · التاريخ/الوقت · نصيحة/رؤى ·
   قدرة الشراء «ينفع أصرف ٥٠٠؟» · أكبر مصروف · أكتر بند · فاضل كام يوم · ميعاد المرتب · صرف شهر محدد ·
   ملخص يومي/أسبوعي/شهري · «ذكّرني بـ…» (يحفظ في الوارد + يجدول إشعار حقيقي) · ردود ودّية.
   - **تجربة الشات**: إدخال صوتي (`speech_to_text` ar_EG، متخفي على الويب) + اقتراحات سريعة +
     **ترحيب استباقي** (أهم تنبيه أول ما يفتح) + **٦ أزرار تنفيذ في الردود** (+مياه/سجّل جرعة/علّم
     عادة/دفعت فاتورة/سجّل مصروف/امسح الوارد) + **ذاكرة سياق** («وأمبارح؟» بعد سؤال مصاريف/مواعيد).
   - **إشعار «ملخص الأسبوع»** كل جمعة ٨م (`WeekSummaryScheduler`، notif id 1070001).
   - Gemini بقى **اختياري** ورا زر ✨ (fallback للأسئلة المفتوحة لو المستخدم حاطط مفتاح).
4. **إعادة تقسيم السايدبار (بحسب مجال الحياة)** — مثبّت فوق ٣ (اليوم/**اسأل مديرك**/الجدول) + ٥ مجموعات:
   الفلوس · الصحة واللياقة · البيت والمشتريات · حياتي وتطوّري · المتابعة والأدوات. مفيش شاشة اتشالت؛
   `top(n)` اتنقلوا جوّه المجموعات. **«اسأل مديرك» ظهر في السايدبار لأول مرة.**
5. **top bar موحّد**: أيقونة **🔍 بحث في كل صفحة** (~٢٨ صفحة اتضافلها `searchAction`) + بحث الرئيسية
   اتنقل من الـ body لأيقونة في الـ AppBar.
6. **الإضافات السريعة في الرئيسية**: اتنقلت **فوق كارت الصلاة** + اتوسّعت **٦→١٣ زرار** (مياه/جرعة/
   تمرين فوري + دخل/قياس/تذكير/اسأل-مديرك + الموجود).

### ب) قرارات جارية / محتاجة قرار المستخدم
- **رفع GitHub + لينك الويب**: الكود والـ CI جاهزين. **محتاج من المستخدم:** (١) رابط ريبو GitHub
  (خاص أو عام) · (٢) Project ID من Firebase (بعد إنشاء المشروع + تفعيل Hosting) لوضعه في `.firebaserc` ·
  (٣) سرّ `FIREBASE_SERVICE_ACCOUNT` في إعدادات الريبو. الخطوات بالتفصيل في `WEB_DEPLOY.md`.
- **شاشة «خصّص الأزرار السريعة»**: مقترحة (يختار المستخدم أي أزرار تظهر + ترتيبها) — لسه ماتعملتش.

### ج) أفكار مستقبلية (للاستكمال)
- **إجراءات سريعة إضافية** مقترحة: نوم · خطوات يدوية · علّم عادة · مشتريات · تسبيح · تحويل بين
  محافظ · فاتورة اتدفعت · دَين/سلفة · صورة مستند.
- **العقل وصل تشبّع** — أي «تزويد» جاي تفاصيل صغيرة؛ الأولوية بقت للنشر/الاختبار الميداني.
- **اختبار ميداني** لكل تدفّقات العقل الجديدة على الجهاز + توسيع تغطية الاختبارات.
- **iOS** مؤجّل (محتاج Mac/Codemagic + Apple Developer).

---

## 0) ريديزاين بريميوم + تعديلات المستخدم (2026-07-09)

المستخدم بعت موكاب داكن أنيق لشاشة «اليوم» وطلب التطبيق يبقى زيه، + تعديلات متتابعة. **الحالة النهائية:**
- **ثيم داكن بريميوم** (`core/theme.dart`): خلفية `#0B0F17`، كروت `#141B27` بحواف مدوّرة ٢٠، أخضر
  نعناعي. الافتراضي داكن (`app_state`). **منتقي لون الهوية** (نعناعي/أزرق/بنفسجي/ذهبي-رمضان) في
  الإعدادات → `AccentPreset`/`kAccentPresets` + `AppState.accentKey` + ValueListenableBuilder ثالث في app.dart.
- **شاشة اليوم:** كارت صلاة «بطل» (تدرّج ليلي + عدّاد تنازلي حي `_CountdownText` + شريط تقدّم + pill) **جنب**
  كارت «ملخص المدير» على الشاشات العريضة (`_heroAndSummary`، LayoutBuilder ≥640). الهيدر تحية خضرا +
  تاريخ (**من غير هلال** — المستخدم شاله). تايلز إجراءات ملوّنة في كارت. شريط صلاة بأيقونات + تمييز الجاية.
  كارت مياه برسمة كوباية (`WaterGlass`) + كارت نوم بموجة (`SleepWave` في `widgets/decorations.dart`).
  النوم من **1→24** ساعة. أنيميشن دخول (`AnimatedSwitcher`). البار العلوي فيه **البحث بس** (اتشال رؤى/بصوتك/وارد).
- **اتشال البار السفلي** (bottom-nav) بالكامل بطلب المستخدم → الشِل رجع بسيط. **مركز التنبيهات**
  (`alerts_center_screen` — بيجمع كل المستحقات) و**لوحة التقارير** (`reports_hub_screen` — إحصائيات/رؤى/
  ملخص الشهر PDF/تقرير الدكتور) اتنقلوا لمجموعة «المراجعة والمتابعة» في الدرج.
- **أرقام إنجليزية** في كل التطبيق (`arNum`→toString + `_west()` للتواريخ؛ الإدخال لسه بيقرا العربي).
- **أيقونة تطبيق adaptive** (هلال أخضر + نجمة على تدرّج كحلي) + **splash** بالهلال (launch_background).
- **بيانات تجريبية:** `core/seed_demo.dart` (`seedDemoData()`) بيملأ ~٣٠ بند؛ زر في الإعدادات → «تجربة».
- **إصلاح قص:** كل الحالات الفارغة (`EmptyHint` بقى محدود العرض + padding) + فورمات الديالوج
  (`scrollable: true` في ١٠ ملفات عشان الكيبورد مايقصّش من تحت).
- **متبقّي ديزاين (assets/شغل أكبر):** glassmorphism · تحديث home widget · صورة مسجد حقيقية. **مفتوح:**
  لو فيه صفحة كاملة لسه مقصوصة من تحت (محتاج سكرين‌شوت — الكود سليم في الـ paddings).

---

## 1) الحالة الحالية (لقطة)

- **الإصدار:** v0.9 (DB schema **v27**) — مثبّت ومتحقّق على الجهاز `RFCW70PZYSZ`.
  **الاختبارات: 145/145 · `flutter analyze` نضيف · logcat نضيف في كل تثبيت.**
  (v26 = form/unit على الأدوية · **v27 = pharmacy_batches**.) بقى **git repo** + إعداد نشر ويب.
- **جداول الميزات (v9→v25):** إيرادات v9 / ملف طبي v10 / جيم v11 / ملابس v12 / ادخار v13 /
  تقدّم بدني v14 / واجبات اجتماعية v15 / صيدلية v16 / ضمانات v17 / عدادات v18 /
  (قرآن+خزنة سرية+إقلاع) v19 / (أقارب+تحديات+كبسولة+travel_min) v20 / (يوميات+وصفات) v21 /
  (محافظ متعددة + wallet_id على المصروف والدخل) v22 / (repeat على المواعيد) v23 / (أصول) v24 /
  **(نباتات البيت) v25**.
- **أفكار مخطط المستخدم (2026-07-08 — الـ٦ كلهم اتعملوا):** محافظ متعددة v22 · مواعيد متكررة v23 ·
  لوحة صحّة موحّدة (بدون DB) · أموالي الخارجية + صافي الثروة v24 · رؤى مستقلة top-level (بند رئيسي
  في الـ shell) · نباتات البيت v25.
- **ميزات بدون DB اتعملت النهارده:** ميزان غذائي · Gemini للملابس · مخ Gemini شامل ·
  صوت الدخل · ربط الساعة بالجيم · **بحث عام** · **لوحة تشغيل سريعة** · **تصدير الشهر PDF** ·
  **بطاقة «دلوقتي»** · **تهيئة أول مرة (Onboarding)** · **شارة السايدبار** · **تقويم النتيجة**
  (+ منتقي شهر/سنة) · سايدبار مجموعات قابلة للطي · **رسم السعرات في الإحصائيات**.
- **المنصة:** Flutter 3.44 + sqflite + محلي بالكامل (لا سحابة). عربي RTL + إنجليزي LTR.
- **اللي اتشحن:** ٧ دفعات + «عقل المدير المجاني» + إعادة هيكلة الواجهة (سايدبار/ثيم/لغة)
  + **الترجمة الإنجليزية الكاملة**.
- **الاختبارات:** 84/84 · `flutter analyze` نظيف · build release APK arm64 (~53.5MB) شغّال.
- **البناء:** `flutter build apk --release --target-platform android-arm64` ثم
  `adb install -r`. **التثبيت بيتم بطلب صريح من المستخدم بس.**

---

## 2) القرارات المتخذة (ثابتة — متتعادش مناقشتها من غير سبب)

### معمارية وتقنية
- **محلي أولًا، بدون سحابة** — كل البيانات SQLite محلي؛ الأمان بالبصمة + نسخ احتياطي ملف/zip.
- **بدون git repo لحد دلوقتي** — القرار مقصود؛ النسخ الاحتياطي عبر ملفات.
- **بناء arm64-only** — الجهاز المستهدف واحد؛ يقلل حجم ووقت البناء.
- **إصلاحات بناء ويندوز load-bearing** (لو اتشالت التطبيق مش هيتبني) — التفاصيل في
  `SESSION_HANDOVER.md`: `flutter_timezone` مثبّت `^3.0.1` · `kotlin.incremental=false` ·
  KGP محقون في module الـ file_picker + خريطة `kotlinJvmTargetFix` لكل module ·
  `receive_sharing_intent` بيحتاج `compileSdk=37` (نسخ `android-37.0`→`android-37`).
- **كراش الـ release إتصلح** (home_widget→glance يجرّ androidx.work/Room قديم + R8 بيشيل
  الـ ctor) — الحل: فرض work 2.9.1 + room 2.6.1 + قاعدة keep في proguard. **درس: أي كراش
  release-only = شك في R8 الأول، واتأكد إن التطبيق فعلًا بيفتح مش بس البناء نجح.**
- **`main.dart`** بينادي `AppState.load()` + `initializeDateFormatting('ar'/'en')` قبل `runApp`.

### اللغة والترجمة (i18n)
- **الترجمة عبر `tr('عربي','English')`** خفيف (مش ARB) بيقرأ `AppState.isEnglish`.
  MaterialActApp بيدعم `[ar,en]` ويقلب الاتجاه تلقائيًا.
- **`core/ar.dart` locale-aware** = نقطة الإصلاح المركزية: `arNum` (لاتيني في الإنجليزي)،
  دوال التاريخ/الوقت، `greetingFor`، `egp` — كلها بتتبع اللغة.
- **`dayKey()` بيفضل لاتيني دايمًا** — مفاتيح الـ DB لازم متتترجمش.
- **قيم الـ DB الثابتة تفضل عربي** وتتعرض عبر helpers (`expenseCategoryLabel` /
  `mealSlotLabel` / `apptCategoryLabel` / `weekdayName` / `prayerNameLabel` / ...).
  السبب: القيمة المخزنة لازم تثبت عبر تغيير اللغة.
- **رسائل الإشعارات اتترجمت** (بتتجدول كل فتحة للتطبيق) — لكن معالج أزرار الإشعار بيطابق
  على الـ **action ID** مش التسمية، فترجمة التسمية آمنة.
- **GOTCHA:** `tr()` مايتحطش جوه widget عليه `const` → شيل الـ const.

### منتج/UX
- **سايدبار Drawer** بدل البار السفلي (زي طارة) + سهم رجوع + همبرجر.
- **الثيم:** نظام/فاتح/غامق عبر `AppState.themeMode`.
- **صوت المدير عامي مصري** في الجُمل الديناميكية (نبرة مقصودة).
- **كارت الطوارئ متاح من شاشة القفل بدون بصمة** (قرار مقصود — وقت الطوارئ بيفرق).

---

## 3) قرارات/بنود لسه عربي عمدًا (متغيّرهاش من غير طلب)

- **`doctor_report.dart` (PDF الطبي)** — مستند للطبيب المصري، RTL ثابت. **أوضح متابعة
  مستقبلية لو المستخدم عايز إنجليزي:** يحتاج ترجمة كل السطور + قلب الاتجاه LTR حسب اللغة.
- **`brain_context.dart`** — النص المُرسل لـ Gemini (مش واجهة مستخدم).
- **خيار اللغة «العربية»** في الإعدادات بيفضل بخطه الأصلي (UX صح — كل لغة باسمها).
- **المحافظة الافتراضية `'القاهرة'`** واتجاه الدين `'لى'`/`'عليا'` — قيم DB.

---

## 4) الجاري / نصف مكتمل

- **لا يوجد شغل كود جاري حاليًا** — الترجمة الإنجليزية اكتملت واتحقق منها.
- **حافة معروفة (منخفضة الأثر):** إشعارات اتجدولت في لغة معينة ثم غيّر المستخدم اللغة
  تفضل باللغة القديمة لحد الفتحة الجاية (بتتجدول مع كل فتحة → بتتصحح تلقائيًا).

---

## 4.1) تكامل الساعة الذكية — القرار والحالة (2026-07-07)

- **القرار:** القراءة عبر **Health Connect** — تكامل واحد يغطي كل ساعات أندرويد
  (سامسونج/Wear OS/Fitbit/Garmin/هواوي-لو-بتصدّر). **مفيش ربط بلوتوث مباشر** لكل ماركة.
- **تم (أندرويد، مجاني):** قراءة الخطوات + النوم (كانوا موجودين) + **الكالوريز
  النشطة + نبض الراحة + المسافة + عدد التمارين** (جديد) — معروضين في قسم «من ساعتك
  الذكية» في شاشة اليوم. مكتبة `health` 13.3.1. best-effort بالكامل.
- **آبل ووتش:** بياناتها في **HealthKit على iOS** فقط → محتاج **نسخة iOS** من التطبيق.
  مكتبة `health` cross-platform فالكود الحالي **هيشتغل زي ما هو على iOS** — لكن بناء
  iOS **مستحيل من ويندوز**: بلوكر **Mac أو Codemagic السحابي + Apple Developer
  ($99/سنة) + TestFlight** (نفس وضع طارة). **مؤجّل لحد ما نعمل مسار iOS.**
- **هواوي:** بيشتغل فقط لو تطبيق Huawei Health على الموبايل بيصدّر لـ Health Connect
  (مش مضمون على كل الأجهزة، خصوصًا اللي من غير Google services).
- **تخزين للرؤى وتقرير الدكتور (تم 2026-07-07، DB v8):** جدول `fitness_logs`
  (يوم/سعرات/مسافة) + `MeasurementsRepo.upsertFitness` (بـ COALESCE عشان الـ null
  مايمسحش قيمة موجودة) + `fitnessSince`؛ `DailyMetrics` بقى فيه calories/distanceKm؛
  الرؤى فيها ارتباط سعرات↔نوم؛ تقرير الدكتور بيطبع متوسط السعرات والمسافة/يوم.
- **متبقّي:** ربط عدد التمارين (`workoutsToday`) بخطة التمرين تلقائيًا؛ رسم بياني
  للسعرات/المسافة في شاشة الرسوم البيانية.

## 5) مؤجّل — بانتظار قرار/إعداد من المستخدم

- **نسخة iOS (تفتح آبل ووتش + مستخدمي آيفون)** — محتاجة Mac أو Codemagic السحابي +
  Apple Developer ($99/سنة) + TestFlight. الكود cross-platform جاهز؛ البلوكر هو البناء.
- **مزامنة تقويم Google** — محتاجة مشروع Google Cloud + OAuth + SHA-1 من المستخدم.
- **محادثة Gemini (عقل المدير التفاعلي)** — شغّالة بس **متجرّبتش**، بتنتظر إن المستخدم
  يضيف مفتاحه المجاني من `aistudio.google.com` (شاشة الإعداد جاهزة).
- **الدفعة ٣ الكاملة (Claude/LLM tool-use)** — الرد ينفّذ فعليًا في الـ DB (إعادة جدولة/
  تسجيل). أصعب جزء = «طبقة الأفعال». تكلفة تشغيل ~١–٥ دولار/شهر. (تفاصيل في ROADMAP).

---

## 6) اختبار على الجهاز لسه مطلوب (اتبنى بس متجرّبش تفاعليًا)

أزرار الإشعارات (اتاخد/تم/اتدفعت) · شارك-للتطبيق · Gemini · الطقس · التذكيرات الشهرية ·
**الوضع الإنجليزي الكامل بعد تثبيت 2026-07-07** (تأكيد بصري من المستخدم مطلوب).

---

## 7) مرشحون للجلسة الجاية (محدّث 2026-07-09)

**اتعمل بالفعل** (كان في القائمة القديمة): الواجبات الاجتماعية · مراجعة القرآن · الضمانات · أهداف
الادخار · العدادات · عداد الإقلاع · يوميات صوتية · وضع السفر · وضع «يوم صعب» · محافظ متعددة ·
أصول/صافي ثروة · مواعيد متكررة · لوحة صحة · نباتات البيت.

**الباقي المرشّح:**
- **بنود ديزاين متبقية:** glassmorphism للكارت البطل · تحديث ودجت الشاشة الرئيسية بالستايل الداكن ·
  صورة مسجد حقيقية في كارت الصلاة (محتاجة asset).
- قراءة رسائل البنك/محفظة الموبايل محليًا → تسجيل مصروف تلقائي (إذن SMS اختياري).
- أظرف المرتب · وضع الجمعة · مسبحة رقمية · (من بنك الأفكار في `ROADMAP_ESTIMATES.md`).
- **تغطية اختبار أوسع** + **تجربة ميدانية** لكل الشاشات (البيانات التجريبية `seedDemoData()` بتسهّلها).

**قاعدة الترتيب المتفق عليها:** الأتمتة اللي بتقلل الإدخال اليدوي قبل الميزات اللي
بتزوّده. أي حاجة أكل/تسوق تفضل بعد ما الصوت/الصور تقوى (الدفعة ٣).

---

## 7.1) خطة عمل معتمدة — 2026-07-07 (٤ دفعات، بالترتيب ده)

المستخدم وافق على التنفيذ **بالترتيب المقترح**، واحدة ورا التانية مع تحقق بعد كل واحدة.

1. ✅ **الإيرادات والمصروفات** (تم 2026-07-07، DB v9): جدولين `income` +
   `recurring_income` + كارت صافي (دخل/مصروف/صافي + نسبة ادخار) + قسم الدخل +
   تذكير مرتب شهري (`incomeNotifId`). **مثبّت؟ لسه — الموبايل كان مفصول.**
2. ✅ **الملف الطبي المتكامل** (تم 2026-07-07، DB v10): `medical_records`
   (visit/lab/imaging/procedure) + صور متعددة + تكلفة→مصروف «صحة» + بند سايدبار +
   قسم في تقرير الدكتور. **مثبّت؟ لسه — الموبايل كان مفصول.**
3. ✅ **الجيم بأوضاع** (تم 2026-07-07، DB v11): `gym_sessions` + `gym_sets` +
   `kGymPrograms` (ppl/upperlower/fullbody/home/cardio → بتكتب في `workout_plan`) +
   مكتبة تمارين + `personalRecords` (PR). **مثبّت + متحقّق.**
4. ✅ **خزانة الملابس + المساعد** (تم 2026-07-07، DB v12): `clothes` +
   `suggestOutfit` (قاعدي بالطقس+الرسمية+الأقل لبسًا — بدون مفتاح). **مثبّت + متحقّق.**

**اقتراحات إضافية:** ✅ **الميزان الغذائي الصافي** (كارت اليوم: أكلت−حرقت + هدف) ·
✅ **أهداف الادخار** (v13) · ✅ **صور التقدّم والمقاسات للجيم** (v14) · ✅ **تحسين اقتراح
الملابس بـ Gemini** (زرار «رأي المساعد الذكي» لو فيه مفتاح) · ✅ **مساعد Gemini شامل**
(brain_context بيضمّ الدخل/الادخار/الجيم/السعرات/الطبي) · ✅ **صوت لتسجيل الدخل** («قبضت
٥٠٠٠ مرتب») · ✅ **ربط `workoutsToday` من الساعة بالجيم auto-mark**. **لسه (اختياري
صغير):** تخزين سعرات/مسافة الجيم في الرؤى كمقياس مستقل · صوت للادخار.

## 7.2) خطة المؤجّلات المعتمدة — 2026-07-08 (١٣ ميزة، بنبنيها على دفعات)

المستخدم اختار ١٣ ميزة من المؤجّلات. **الدفعة ١ اتعملت + اتحقّقت (DB v19)؛ الباقي جاي.**

**✅ اتعمل (الدفعة ١):**
1. رسم السعرات المحروقة في الإحصائيات (بدون DB).
2. ⭐ مراجعة القرآن بالتكرار المتباعد (v19، `quran_reviews` + جدول تباعد + تذكير ٨م).
3. خزنة ملاحظات سرية ورا البصمة (v19، `secret_notes` + `LocalAuthentication` — وصول
   محمي بالبصمة مش تشفير فعلي).
4. عدّاد الإقلاع (v19، `quit_counters` + أيام + توفير + reset).

**✅ اتعمل كمان (الدفعات ٢ + ٣ — الـ١٣ كلهم خلصوا في الكود، DB v20+v21):**
- صلة الرحم (v20) · تحديات شهرية (v20) · كبسولة زمنية (v20) · «اتحرك دلوقتي» (travel_min على المواعيد) ·
  يوميات صوتية (v21) · دفتر الوصفات (v21) · وضع «يوم صعب» (setting) · وضع السفر (setting) ·
  استيراد أعياد الميلاد (dep `flutter_contacts` + READ_CONTACTS). **البناء نجح، 100/100.**
  **مثبّت + متحقّق على الجهاز 2026-07-08 (ترقية v18→v21 اشتغلت نضيف + flutter_contacts شغّال).**

**~~باقي~~ (اتعمل كله):**
5. **صلة الرحم الدورية** — جدول أقارب (اسم/تليفون/كل كام يوم/آخر اتصال) + تذكير دوري (زي
   `home_maintenance`).
6. **تحديات شهرية** — تحدّي ٣٠ يوم بعدّاد وشارة (جدول challenges أو إعادة استخدام habits).
7. **كبسولة زمنية** — رسالة لنفسك بتتفتح في تاريخ مستقبلي + تذكير.
8. **«اتحرك دلوقتي»** — عمود `travel_min` على المواعيد + إشعار تاني وقت التحرك (موعد − مدة المشوار).
9. **يوميات صوتية** — تسجيل صوتي يومي + تفريغ عبر `speech_to_text` مخزّن كنص (جدول diaries).
10. **دفتر وصفات البيت** — جدول recipes (اسم/صورة/مقادير/خطوات) + «ضيف المقادير للتسوق».
11. **وضع «يوم صعب»** — toggle في الإعدادات يهدّي التطبيق (يخفي الضغط/الملخص الصارم).
12. **وضع السفر** — toggle يوقف تذكيرات الروتين مؤقتًا.
13. **استيراد أعياد الميلاد من جهات الاتصال** — **يحتاج مكتبة `flutter_contacts` + إذن
    Contacts** (بلوكر بسيط) → يكتب في جدول `occasions` الموجود. **يُعمل آخر واحد.**

**قاعدة:** كل دفعة = DB bump (لو محتاج) + موديل + repo + شاشة + بند سايدبار + تذكير (لو مناسب)
+ اختبار migration + analyze/test/build/install/verify.

## 8) طريقة الاستئناف (checklist سريع)

1. اقرأ الملف ده + `SESSION_HANDOVER.md` (أول قسم).
2. `flutter analyze` + `flutter test` (حاليًا **186/186**) قبل أي تغيير.
3. اشتغل — أي نص جديد: `tr('عربي','English')` + شيل const لو فيه tr. أي DB جديد = bump نسخة
   + `_vNN Tables` في `db.dart` (migration + createSchema loop) + اختبار «vXX ← vYY».
4. `flutter build apk --release --target-platform android-arm64`.
5. **اطلب إذن** قبل `adb install -r` والتشغيل على الجهاز، وتأكد إنه بيفتح (`adb shell pidof`).
   **ملاحظة:** الجهاز `RFCW70PZYSZ` بيفصل من USB كتير — لو التثبيت فشل، استنى يتوصّل وأعد.
6. حدّث `SESSION_HANDOVER.md` + الملف ده + ملف الذاكرة.
