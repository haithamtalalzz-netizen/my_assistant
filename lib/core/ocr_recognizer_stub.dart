/// نسخة الويب: قراءة الصور (ML Kit) مش متاحة في المتصفح، فبنرجّع null
/// ويتعامل باقي الكود كأن القراءة فشلت (المستخدم يكتب القيمة بنفسه).
Future<String?> recognizeFromPath(String path) async => null;
